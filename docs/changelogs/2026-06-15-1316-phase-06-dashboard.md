# Phase 6 — Dashboard Frontend

**Date:** 2026-06-15  
**Branch:** `worktree-phase-06-dashboard`

## Summary

Implemented the React + Vite + TailwindCSS + shadcn/ui dashboard for JiGit per `docs/plans/phase-06-dashboard.md`.

## What changed

### New files

- `packages/dashboard/src/api/client.ts` — fetch wrappers (`listJobs`, `getJob`, `controlJob`, `decideApproval`) and `useSSE` hook for live job events.
- `packages/dashboard/src/api/client.test.ts` — TDD tests for the API client.
- `packages/dashboard/src/components/ui/*` — shadcn/ui components: button, badge, card, table, dialog, tabs, skeleton, alert, tooltip, scroll-area, separator, accordion.
- `packages/dashboard/src/components/JobStatusBadge.tsx` — status badge with `STATUS_VARIANT` map.
- `packages/dashboard/src/components/StatCard.tsx` — metric card with icon/value/label.
- `packages/dashboard/src/components/ApprovalCard.tsx` — pending approval card with approve/reject actions.
- `packages/dashboard/src/components/EventRow.tsx` — single event log row.
- `packages/dashboard/src/components/layout/AppShell.tsx` — sidebar navigation + header layout.
- `packages/dashboard/src/pages/Overview.tsx` — mock metrics + Recharts LineChart/PieChart + recent activity.
- `packages/dashboard/src/pages/Jobs.tsx` — jobs list table with stop/pause/resume actions.
- `packages/dashboard/src/pages/JobDetail.tsx` — tabs (timeline/events/raw), SSE events, pending approvals sidebar.
- `packages/dashboard/src/pages/Config.tsx` — read-only config view for templates, credentials, repo mappings.
- `packages/dashboard/src/App.tsx` — router setup.
- `packages/dashboard/src/lib/utils.ts` — `cn()` helper.
- `packages/dashboard/src/index.css` — Tailwind directives + shadcn CSS variables.
- `packages/dashboard/tailwind.config.js` — Tailwind v3 config with shadcn color tokens and accordion keyframes.
- `packages/dashboard/postcss.config.js` — PostCSS config for Tailwind + autoprefixer.
- `docs/plans/phase-06-dashboard.md.tasks.json` — persisted task list for cross-session resume.

### Modified files

- `packages/dashboard/package.json` — added dependencies: `react-router-dom`, `recharts`, `class-variance-authority`, `clsx`, `tailwind-merge`, `tailwindcss-animate`, `lucide-react`, Radix primitives; added dev deps: `tailwindcss@3`, `postcss`, `autoprefixer`.
- `packages/dashboard/tsconfig.json` — added `@/*` path alias.
- `packages/dashboard/vite.config.ts` — added `@` alias and API proxy to `localhost:3000`.
- `packages/dashboard/src/main.tsx` — bootstrap React app with `index.css` import.
- `pnpm-lock.yaml` — lockfile updated for new dependencies.

## Tests

- Added `src/api/client.test.ts` (4 tests) — all passing.
- Verified `pnpm --filter @jigit/dashboard test` passes.
- Verified `pnpm --filter @jigit/dashboard build` produces static assets in `dist/`.
- Verified full monorepo test suite passes after `pnpm -r build`.

## Follow-ups

- Hook up real ingestion for Overview charts in Phase 2.
- Add end-to-end tests for the dashboard once the API is stable.
- Consider code-splitting the main bundle (current build warns about a >500 kB chunk from Recharts).
